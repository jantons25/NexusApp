# MongoDB Connection
MONGODB_URI=mongodb://localhost/merndb

# Server Configuration
PORT=4000
NODE_ENV=development

# Frontend URL (CORS)
FRONTEND_URL=http://localhost:5173

# Optional: JWT Secret (si usas autenticación)
JWT_SECRET=cambia-esto-por-un-secreto-seguro